import styles from "/src/styles/title-bar.module.css";

function TitleBar() {
    return (
        <header className="title-bar">
            <h1> Cloud </h1>
        </header>
    );
}   

export default TitleBar;